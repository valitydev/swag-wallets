name: Release Erlang Libraries

on:
  push:
    branches:
      - 'v1'
      - 'epic/**'
  pull_request:
    branches: ["**"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4

      - name: Setup swagger-codegen
        uses: valitydev/action-setup-swagger-codegen@v0.0.4
        with:
          codegen-version: "2.4.25"
          generator-version: "1.1.0"

      - name: Install deps
        run: npm install

      - name: Bundle spec and build docsite
        run: npm run build

      - name: Generate stubs
        run: |
          mkdir -p out
          echo "Generate wallet stubs"
          swagger-codegen generate -l vality-erlang-client \
            -i web_deploy/swagger.json -o out/erlang-client-wallet \
            --additional-properties packageName=swag_client_wallet
          swagger-codegen generate -l vality-erlang-server \
            -i web_deploy/swagger.json -o out/erlang-server-wallet \
            --additional-properties packageName=swag_server_wallet
          echo "Generate payres stubs"
          swagger-codegen generate -l vality-erlang-client \
            -i web_deploy/payres/swagger.json -o out/erlang-client-payres \
            --additional-properties packageName=swag_client_payres
          swagger-codegen generate -l vality-erlang-server \
            -i web_deploy/payres/swagger.json -o out/erlang-server-payres \
            --additional-properties packageName=swag_server_payres

      - name: Set release suffix
        id: vars
        run: |
          BRANCH_NAME="${GITHUB_REF##*/}"
          if [[ "$GITHUB_REF" == refs/heads/epic/* ]]; then
            RELEASE_SUFFIX="epic-${BRANCH_NAME}"
          else
            RELEASE_SUFFIX="v1"
          fi
          echo "release_suffix=${RELEASE_SUFFIX}" >> $GITHUB_OUTPUT

      - name: Deploy erlang-client-wallet artifact to release branch
        if: ${{ github.event_name == 'push' }}
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          branch: >-
            release/erlang/wallet-client/${{ steps.vars.outputs.release_suffix }}
          folder: out/erlang-client-wallet
          commit-message: Release ${{ github.repository }}@${{ github.sha }}

      - name: Deploy erlang-server-wallet artifact to release branch
        if: ${{ github.event_name == 'push' }}
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          branch: >-
            release/erlang/wallet-server/${{ steps.vars.outputs.release_suffix }}
          folder: out/erlang-server-wallet
          commit-message: Release ${{ github.repository }}@${{ github.sha }}

      - name: Deploy erlang-client-payres artifact to release branch
        if: ${{ github.event_name == 'push' }}
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          branch: >-
            release/erlang/payres-client/${{ steps.vars.outputs.release_suffix }}
          folder: out/erlang-client-payres
          commit-message: Release ${{ github.repository }}@${{ github.sha }}

      - name: Deploy erlang-server-payres artifact to release branch
        if: ${{ github.event_name == 'push' }}
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          branch: >-
            release/erlang/payres-server/${{ steps.vars.outputs.release_suffix }}
          folder: out/erlang-server-payres
          commit-message: Release ${{ github.repository }}@${{ github.sha }}
