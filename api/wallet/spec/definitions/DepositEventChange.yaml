description: |
  Изменение, возникшее в процессе ввода средств
type: object
discriminator: type
required:
  - type
properties:
  type:
    description: Тип произошедшего изменения
    type: string
    enum:
      - DepositStatusChanged
