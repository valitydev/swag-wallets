description: Ресурс для осуществления платежей
type: object
discriminator: type
required:
  - type
properties:
  type:
    description: |
      Тип платёжного ресурса.

      См. [RBKmoney Payment Resource API](/?api/payres/swagger.yaml).
    type: string
    enum:
      - BankCardPaymentResource
      - BankWirePaymentResource
