---
description: Receiver resource parameters
type: object
discriminator: type
required:
  - type
properties:
  type:
    description: |
      The resource type of the receiver.

      See [Vality Withdrawal Resource API](?api/payres/swagger.yaml).
    type: string
    enum:
      - BankCardReceiverResourceParams
