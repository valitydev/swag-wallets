type: object
required:
  - reportType
  - fromTime
  - toTime
properties:
  reportType:
    description: Тип отчета
    type: string
    enum:
      - withdrawalRegistry
  fromTime:
    description: Начало временного отрезка
    type: string
    format: date-time
  toTime:
    description: Конец временного отрезка
    type: string
    format: date-time
