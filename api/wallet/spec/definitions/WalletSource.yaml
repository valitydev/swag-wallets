description: Кошелёк как источник средств для вывода
type: object
required:
  - walletID
properties:
  walletID:
    x-rebillyMerge:
      - $ref: '#/definitions/WalletID'
  walletGrant:
    x-rebillyMerge:
      # - $ref: '#/definitions/WalletGrantToken'
      - $ref: '#/definitions/GrantToken'
