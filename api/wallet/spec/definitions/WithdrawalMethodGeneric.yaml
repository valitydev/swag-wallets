type: object
allOf:
  - $ref: '#/definitions/WithdrawalMethod'
  - type: object
    required:
      - providers
    properties:
      providers:
        description: Список обобщенных провайдеров
        type: array
        items:
          x-rebillyMerge:
            - $ref: '#/definitions/GeneticProvider'