get:
  operationId: getDepositEvents
  summary: Получить событие ввода средств
  tags:
    - Deposits
  parameters:
    - $ref: '#/parameters/requestID'
    - $ref: '#/parameters/depositID'
    - $ref: '#/parameters/eventID'
  responses:
    '200':
      description: Событие найдено
      schema:
        $ref: '#/definitions/DepositEvent'
    '400':
      $ref: '#/responses/BadRequest'
    '404':
      $ref: '#/responses/NotFound'
