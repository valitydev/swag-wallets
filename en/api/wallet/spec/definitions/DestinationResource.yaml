---
description: Asset receiver resource used to make withdrawals
type: object
discriminator: type
required:
  - type
properties:
  type:
    description: |
      The resource type of the asset receiver.

      See. [Vality Payment Resource API](?api/payres/swagger.yaml).
    type: string
    enum:
      - BankCardDestinationResource
      - CryptoWalletDestinationResource
      - DigitalWalletDestinationResource
