---
type: object
required:
  - url
  - expiresAt
properties:
  url:
    description: URL of the file
    type: string
  expiresAt:
    description: Time until which the link will be considered valid
    type: string
    format: date-time
