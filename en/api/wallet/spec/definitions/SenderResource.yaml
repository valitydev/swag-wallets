---
description: The sender resource used to make transfers
type: object
discriminator: type
required:
  - type
properties:
  type:
    description: |
      The resource type of the sender of the funds.

      See. [Vality Payment Resource API](?api/payres/swagger.yaml).
    type: string
    enum:
      - BankCardSenderResource
